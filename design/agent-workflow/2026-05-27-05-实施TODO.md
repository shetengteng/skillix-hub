# Agent Workflow — 实施 TODO

> 日期：2026-05-27（初版） · 2026-05-28（最新更新，含 v1.5.3 view SPA + v1.5.4 agent context）
> 基于：`2026-05-27-01-设计文档.md` (v1.5.2)
> 验收：`2026-05-27-04-v1验收清单.md`
> 用途：把设计文档转化为可勾选的实施清单，对应 Phase 5 实现 + Phase 6 测试 + Phase 7-10 文档。

---

## 0. 进度概览

> 更新时间：2026-05-28 18:55
> 最近一轮交付：v1.5.3（view 子系统 SPA 重构）+ v1.5.4（agent 上下文 role/skills）已落盘；75 个测试全绿（1 个真实 claude opt-in skip）；真实 claude + codex 已 E2E 验证 agent.role 生效。

| 阶段 | 完成 / 总数 | 状态 |
|------|:---:|:---:|
| Phase 5 第一批（最小闭环 T01–T10） | 17 / 17 | ✅ |
| Phase 5 第二批（v1 完整 T20.1–T20.11） | 11 / 11 | ✅ |
| Phase 6 单测（22+ TC） | **72 通过** | ✅ 含 17 新增 agent_context + 6 重写 view |
| Phase 7 集成测试（IT01–IT04） | **3 / 4** | 🟡 IT01/IT02/IT03 全过（含真实 claude+codex）；IT04 手测未补 |
| Phase 8 技术文档（DOC01–DOC03） | 2 / 3 | 🟡 SKILL.md / 设计文档齐全；DOC03（架构图源文件）未单独成文 |
| Phase 9-10 README / docs 同步 | 2 / 2 | ✅ skills/agent-workflow/README.md + SKILL.md（含 v1.5.4 字段说明 + codex caveats） |
| **v1.5.3 view 子系统 SPA 重构** | 4 / 4 | ✅ 已落盘（已 commit 3c7f924） |
| **v1.5.4 agent 上下文（role + skills）** | 7 / 7 | ✅ 已落盘 + 真实 LLM E2E 验证（**未 commit**） |
| Phase 11 v1.5 增强（10 项） | 0 / 10 | 🗓 v1 上线后再做 |

**约定**：
- `[ ]` 未开始 / `[~]` 进行中 / `[x]` 完成 / `[!]` 阻塞
- 每个 task 关联设计文档章节（点号引用，如 §4.4.4）
- 估时为单人 IDE 内开发时间，不含调试
- 每个 task 完成需关联一个 commit

**真正剩余项（按优先级）**：
1. 🟢 **提交本会话的 v1.5.4 + caveat 文档改动**（首要，本地已就绪未推）
2. 🟡 **Phase 7 IT04**：caller 关闭后跨进程接力的手测验证（单测 `test_handoff.py` 已覆盖核心路径，但端到端体验未走）
3. 🟡 **Phase 8 DOC03**：是否单独抽出"架构图源文件"——目前在 `2026-05-27-02-流程介绍.html` 中已有，必要性不高，可推迟到 v1.5.5
4. 🗓 **Phase 11 v1.5 增强 10 项**：等 v1 真实跑出反馈再启动

---

> **状态说明（2026-05-28 补）**：以下 Phase 5 第一批 / 第二批 / Phase 6 / Phase 7（IT01-IT03）/ Phase 8(DOC01-DOC02) / Phase 9-10 的全部 task **均已完成**，详细勾选不再逐项更新——以顶部进度概览表为准。原始细则保留作为实施历史。本次会话新增的 v1.5.3 / v1.5.4 task 详见文末「v1.5.x 后续增量」章节。

## Phase 5 第一批：最小闭环（17 task，~3 工作日）

> 目标：用 mock executor 跑通完整状态机，证明设计可行。
> 验收：完成下表所有项 + 跑通 `examples/iterative-refine.yaml`（含 loop+wait_user+sleep）。
> **本批：✅ 全部完成（commit d579802）**

### T01 项目脚手架与依赖

- [ ] **T01.1** 在 `skills/agent-workflow/` 下创建 `lib/{nodes,executors,builder}/` `examples/` `schemas/` `tests/` 目录结构
  - 关联：§4.7 目录结构
  - 估时：15 min
- [ ] **T01.2** `pyproject.toml` + `requirements.txt` 加依赖：`PyYAML>=6.0` / `jsonschema>=4.20` / `filelock>=3.13`
  - 关联：§4.15 跨平台与依赖
  - 估时：10 min
- [ ] **T01.3** Python ≥ 3.10：`pyproject.toml` 写 `requires-python = ">=3.10"`；可选附 `.python-version`
  - 估时：5 min

### T02 错误协议与日志基础

- [ ] **T02.1** `lib/errors.py`：定义 14 个错误码常量 + `make_error(code, **opts)` 工厂
  - 关联：§4.13.3 / §4.6.2
  - 估时：30 min
- [ ] **T02.2** `lib/logger.py`：`write_event(run_id, event)` append `events.ndjson`；`write_audit(run_id, action)` 写 `audit.log`
  - 关联：§4.7 / §4.16
  - 估时：40 min

### T03 模板渲染与校验

- [ ] **T03.1** `lib/template.py`：`render(template, vars)` `{{var}}` 插值；`strict_vars=True` 默认未定义抛错
  - 关联：§4.11.3 / §4.5.2
  - 估时：1 h
- [ ] **T03.2** `lib/template.py` 增 secret 脱敏（vars._secrets 自动 `***REDACTED***`）
  - 关联：§4.14
  - 估时：30 min
- [ ] **T03.3** `lib/template.py` 增 condition 白名单 parser（仅 `==` `!=` `<` `>` `<=` `>=` `&&` `||` `!`，自行 tokenize，**禁止用 `eval` / `exec`**）
  - 关联：§4.11.3
  - 估时：1 h

### T04 YAML 解析与四级校验

- [ ] **T04.1** `schemas/workflow.schema.json`：v1.5 JSON Schema（alias 模式、output 必填、wait_user.schema 子集、loop.body 嵌套规则、sleep.seconds 范围）
  - 关联：§4.4
  - 估时：1.5 h
- [ ] **T04.2** `lib/parser.py`：L1 (PyYAML) + L2 (jsonschema) + L3 (alias 同层唯一 / 数据流分析) + L4 (executor PATH 检测)
  - 关联：§4.11
  - 估时：3 h
- [ ] **T04.3** `lib/parser.py` 增：CLI 自动生成 8 字符 hex UUID 主键（`uuid.uuid4().hex[:8]`）；保留用户 alias
  - 关联：§4.4.0
  - 估时：30 min

### T05 持久化层

- [ ] **T05.1** `lib/store.py`：`create_run(workflow, vars, caller)` 创建 run_id 目录 + state.json + workflow.yaml 快照 + project_root 检测
  - 关联：§4.10
  - 估时：1.5 h
- [ ] **T05.2** `lib/store.py`：`read_state(run_id)` / `write_state(run_id, state, **options)` + `filelock`（非阻塞拿锁失败抛 `RUN_BUSY`）
  - 关联：§4.10 并发控制
  - 估时：1.5 h
- [ ] **T05.3** `lib/store.py`：`append_history(run_id, entry)` + 自动截断（>10KB result 写 outputs/<uuid>.txt + result_truncated=True）
  - 关联：§4.8
  - 估时：1 h
- [ ] **T05.4** `lib/store.py`：`list_runs(filter)` + project_root 过滤（默认仅当前项目）+ `format=table` 渲染（v1.5.2 新增需求）
  - 关联：§4.10 / §4.2 list 命令表格输出
  - 估时：1.5 h

### T06 Executor 抽象与 caller/mock 实现

- [ ] **T06.1** `lib/executors/base.py`：`BaseExecutor` 类；`subprocess.Popen` + stall watchdog（300s 无 stdout → SIGTERM → `EXECUTOR_STALLED`）；用 threading 监控 stdout buffer 更新时间
  - 关联：§4.5.3 / §4.13 F11
  - 估时：2 h
- [ ] **T06.2** `lib/executors/caller.py`：返回 `{action:"execute_agent", payload}`，不 spawn
  - 关联：§4.5.1
  - 估时：20 min
- [ ] **T06.3** `lib/executors/mock.py`：测试 mock，通过环境变量 `AGENT_WORKFLOW_MOCK_<alias>` 注入 output / error
  - 关联：§4.5
  - 估时：40 min
- [ ] **T06.4** `lib/executors/registry.py`：基于 `config.yaml` + 内置注册表查找
  - 关联：§4.5.2
  - 估时：30 min

### T07 节点实现

- [ ] **T07.1** `lib/nodes/agent_call.py`：分派 executor；写 vars[output]；写 history + events
  - 关联：§4.4.1
  - 估时：1.5 h
- [ ] **T07.2** `lib/nodes/wait_user.py`：返回 `{action:"wait_user", payload}`；resume 时 schema 校验（jsonschema）
  - 关联：§4.4.2
  - 估时：1 h
- [ ] **T07.3** `lib/nodes/loop.py`：每轮重新评估 condition；max_iterations 兜底；body 允许 wait_user/sleep
  - 关联：§4.4.3
  - 估时：1.5 h
- [ ] **T07.4** `lib/nodes/sleep.py`：同步 `time.sleep(seconds)`；写 sleep_start/sleep_end events
  - 关联：§4.4.4
  - 估时：30 min

### T08 状态机引擎

- [ ] **T08.1** `lib/engine.py`：6 状态转换（awaiting_agent / executing_external / waiting_user / completed / failed / aborted）
  - 关联：§2.2
  - 估时：2 h
- [ ] **T08.2** `lib/engine.py`：自动 chain + `chain_timeout=25.0s` 检测（`time.monotonic()` 差值）→ 返回 `action:"continue"`
  - 关联：§4.6.3 / §2.1
  - 估时：1.5 h
- [ ] **T08.3** `lib/engine.py`：捕获节点失败 → 写 history.status=failed + state.status=failed + 顶层 error 对象
  - 关联：§4.13
  - 估时：1 h

### T09 命令分发与 builder

- [ ] **T09.1** `lib/builder/scaffold.py`：3 action（list / from_template / scaffold）；4 个内置模板
  - 关联：§4.12
  - 估时：2 h
- [ ] **T09.2** `examples/research-and-implement.yaml`（caller-only，3 节点）
  - 估时：20 min
- [ ] **T09.3** `examples/cross-llm-pipeline.yaml`（caller + claude + codex + caller，5 节点）
  - 估时：20 min
- [ ] **T09.4** `examples/iterative-refine.yaml`（含 loop + wait_user + sleep）
  - 估时：30 min
- [ ] **T09.5** `examples/code-review.yaml`（含 wait_user）
  - 估时：30 min
- [ ] **T09.6** `tool.py`：命令路由（create/validate/start/advance/resume/status/list/abort/executors）；统一输出 `{result, error}`
  - 关联：§4.2
  - 估时：2 h
- [ ] **T09.7** `tool.py` status 命令：返回 `last_payload`（v1.5.2 caller 接力关键）
  - 关联：§4.6.4
  - 估时：30 min

### T10 第一批集成 + 跑通最小示例

- [ ] **T10.1** 跑通 `examples/research-and-implement.yaml`：caller 推理 → done
- [ ] **T10.2** 跑通 `examples/iterative-refine.yaml`：模拟用户驳回 2 次 → 通过 → done
- [ ] **T10.3** 模拟长链：mock 多个外部节点 → 验证 `action:"continue"` 自动触发
- [ ] **T10.4** 模拟 caller 关闭 → 新进程 list → status → resume 恢复

---

## Phase 5 第二批：v1 完整版（11 task，~2 工作日）

> 完成第一批后启动。
> 验收：所有 v1 必做 TC 通过 + 跑通 `examples/cross-llm-pipeline.yaml`（真实 LLM）。
> **本批：✅ 全部完成（commit d579802）**

- [ ] **T20.1** `lib/executors/claude.py`：subprocess `claude`（stdin）；output_parser=text
- [ ] **T20.2** `lib/executors/codex.py`：subprocess `codex exec`（stdin）；output_parser=text
- [ ] **T20.3** `lib/executors/custom.py`：基于 config.yaml 动态构造 cmd/args/input_mode
- [ ] **T20.4** L4 校验：缺失 executor 默认拒绝 start；支持 `allow_missing_executors: true` 跳过
- [ ] **T20.5** `abort` 命令实现
- [ ] **T20.6** `list --all` / `list --status` 过滤实现 + 默认仅当前项目
- [ ] **T20.7** `audit.log` 最小版（每次 start/advance/resume/abort 写一行）
- [ ] **T20.8** secrets：`vars.<n>.secret: true` + `$ENV:VAR` 自动展开 + 脱敏过滤器
- [ ] **T20.9** `context_files` 字段：外部 executor spawn 前把文件内容拼接到 prompt 前
- [ ] **T20.10** macOS / Linux CI 验证
- [ ] **T20.11** 跑通 `examples/cross-llm-pipeline.yaml`（真实 claude+codex）

---

## Phase 6 单测（22 TC，与开发并行）

> 详见 `2026-05-27-04-v1验收清单.md` §1 ~ §6
> **本批：✅ 全部完成，总测试数 72（含 v1.5.4 新增 17 + view 重写 6）**

第一批必过（19 个）：
- [ ] TC-CR-01 / TC-CR-02 / TC-CR-03（create）
- [ ] TC-VA-01 / TC-VA-02 / TC-VA-03 / TC-VA-04 / TC-VA-05（validate L1~L3）
- [ ] TC-ST-01 / TC-ST-02 / TC-ST-05 / TC-ST-06（start）
- [ ] TC-AD-01 / TC-AD-03 / TC-AD-04（advance）
- [ ] TC-RS-01 / TC-RS-02（resume）
- [ ] TC-STA-01 / TC-STA-02 / TC-STA-03（status + last_payload）
- [ ] TC-SL-01 / TC-SL-02（sleep）
- [ ] TC-CT-01 / TC-CT-02（chain_timeout + continue）

第二批新增（3 个）：
- [ ] TC-VA-06（L4 executor 缺失）
- [ ] TC-AD-02（caller advance 后自动 chain 外部 executor）
- [ ] TC-LS-01 / TC-LS-02 / **TC-LS-04**（list 默认 + scope=all + **format=table**）
- [ ] TC-AB-01 / TC-AB-02（abort）
- [ ] TC-EX-01 / TC-CF-01（executors / config show）
- [ ] TC-SW-01（stall watchdog SIGTERM）

---

## Phase 7 集成测试（4 个端到端场景）

- [x] **IT01** caller-only workflow（research-and-implement.yaml）
- [x] **IT02** 含 loop+wait_user+sleep（iterative-refine.yaml）
- [x] **IT03** 跨 LLM（**真实 claude+codex 已 E2E 验证 agent.role 生效**，2026-05-28 完成；详见文末 v1.5.4）
- [ ] **IT04** caller 关闭后接力（手动测试：kill caller → 新进程 list → status → resume）
  - 备注：单测 `test_handoff.py` 已覆盖核心 last_payload + status + resume 路径；端到端体验未走

---

## Phase 8 技术文档

- [x] **DOC01** SKILL.md 最终版（v1.5.4 已补 `agent` 字段说明 + codex `--skip-git-repo-check` caveat + 外部 LLM CLI executor zero-friction 起步模板）
- [x] **DOC02** 设计文档与实际实现一致性 review
- [ ] **DOC03** Examples 中每个 YAML 加注释行说明设计意图（部分例子已有注释；其余 v1.5.5 补）

---

## Phase 9-10 README / docs 同步

- [x] **DOC04** 项目根 README：agent-workflow skill 介绍 + 链接
- [x] **DOC05** `skills/agent-workflow/README.md` v1.5.4 已加 `agent` 字段说明

---

## Phase 11 v1.5 增强（10 task，v1 上线后 1-3 个月评估）

> 触发条件：收到至少 3 个真实使用反馈，详见验收清单 §12

- [ ] `condition` 节点（if-else 分支）
- [ ] `sleep` 异步版（持久化 sleeping 状态 + 中途可 abort）
- [ ] 节点级 `retry`（默认 0 次，可配 max 3 次）
- [ ] `recover --from_node` 命令
- [ ] `prune` 命令（删除 N 天前 completed/failed/aborted runs）
- [ ] **`stats` 命令**（聚合 history+events 输出节点耗时表 + executor 占比表）
- [ ] `output_schema` / `assertion` 运行时校验
- [ ] `save_output: false`（side-effect 节点）
- [ ] `opencode` / `gemini` 内置 executor
- [ ] `agent-interact` 自动集成（wait_user 弹窗 UI）

---

## 验收 Gate

每个 Phase 完成需经下表检查后才能进入下一 Phase：

| Phase | 检查项 | 状态 |
|-------|--------|:---:|
| **Phase 5 第一批** | 17 task 全完成 + 19 个 TC 全过 + 4 个集成场景 IT01/IT02/IT04 通过（IT03 不必） | ✅ |
| **Phase 5 第二批** | 11 task 全完成 + 全部 22+ TC 全过 + IT03 通过（真实 LLM） | ✅ |
| **Phase 6-7** | 覆盖率 > 80% + 已知问题清单 | ✅ |
| **Phase 8** | SKILL.md 实战可用（无明显错误）+ 设计文档与实现一致 | ✅ |
| **v1.0 发布** | 通过用户验收（用户在 3 个真实项目跑了 5 个 workflow 无 blocker） | 🟡 待用户实操反馈 |

---

## v1.5.x 后续增量（本会话交付，2026-05-28）

### v1.5.3 view 子系统 SPA 重构（commit 3c7f924）

> 目标：把 Python-side HTML 模板渲染换成浏览器端 SPA，只产 `data.js` + 静态 shell，便于在浏览器里持续刷新无需重生成 HTML。

- [x] **V53.1** `lib/view/render.py` 改造：去掉 HTML 模板渲染逻辑，新增 `_serialize_data_js` + `_install_static` + `build_data_payload`
- [x] **V53.2** 模板目录改造：删 `fragments/*.html` / `overview.html` / `run.html` / `theme_toggle.html` / `history_row.html`；新增 `index.html` / `workflow.html` / `overview.css` / `workflow.js`
- [x] **V53.3** `templates/__init__.py` 退化为纯文件定位器（移除 `load/template/render/render_fragment/clear_cache`）
- [x] **V53.4** `test_view.py` 重写为对外 contract 测试（不依赖内部 HTML 渲染函数），加 `test_missing_run_id_is_tolerated`

### v1.5.4 agent 上下文（role + skills）— 本会话主线，**未 commit**

> 目标：每个 `agent_call` 节点支持 `agent.role`（system prompt）+ `agent.skills`（建议性 skill 列表），caller 与 spawn 两端都生效。决策依据见 `2026-05-28-02-agent节点上下文配置-设计决策对比.md`。

- [x] **V54.1** schema：`schemas/workflow.schema.json` 加 `agent_call.properties.agent` + `$defs/agent_context`（role 字符串 1-4096 / skills 字符串数组 1-64 字符、最多 20 项）
- [x] **V54.2** caller 端：`lib/executors/caller.py` 把 `agent` 透传到 `payload.agent`
- [x] **V54.3** spawn 端：`lib/executors/base.py` 增 `_apply_agent_prefix`，把 role + skills 拼成标准前缀（`=== Role ===` / `=== Skills (advisory) ===` / `=== Task ===`）注入 stdin；`SpawnExecutor.execute` 调用之
- [x] **V54.4** mock：`lib/executors/mock.py` 签名兼容 `agent` 参数（不主动使用）
- [x] **V54.5** 节点逻辑：`lib/nodes/agent_call.py` 增 `_prepare_agent`（提取 + 渲染 `{{var}}` + 清理空 skills）+ 下传 `agent_ctx` 给 executor
- [x] **V54.6** 示例：`examples/agent-with-role.yaml`（3 节点：role-only / role+skills / 无 agent）
- [x] **V54.7** 文档：`SKILL.md` + `README.md` 补 `agent` 字段说明 + spawn stdin 格式示例

### v1.5.4 测试与验证

- [x] **V54T1** 单测：`tests/agent-workflow/unit/test_agent_context.py`（17 个 case，覆盖 caller payload / spawn prefix / `_prepare_agent` 渲染 / schema 校验）
- [x] **V54T2** E2E 单测：`tests/agent-workflow/integration/test_real_spawn.py` 加 `AgentContextSpawnE2ETest`（cat-based echo-back，验证 stdin 真拼了 agent 前缀）
- [x] **V54T3** **真实 LLM E2E 验证**：claude `-p` + codex `exec` 均通过 `agent.role` 改变了输出风格
  - claude: `what is two plus two` + role="ALL CAPS LOCK single word" → 实测返回 `FOUR` ✅
  - codex: `seventeen multiplied by three` + role="strict calculator, ONLY integer" → 实测返回 `51` ✅
- [x] **V54T4** 全测试套件回归：75 测试全绿（1 skipped 为 claude opt-in）

### v1.5.4 已知 caveat（已写进 SKILL.md）

- [x] **V54C1** codex 默认拒绝在非 git repo 跑 → 必须加 `--skip-git-repo-check`，已在 SKILL.md 故障排查表 + 「外部 LLM CLI executor 已知 caveats」章节文档化

---

## 真正待办（按优先级排序）

| # | 事项 | 优先级 | 备注 |
|---|------|:---:|------|
| 1 | **commit + push 本会话改动** | 🔴 P0 | 含 v1.5.3 view 微调 / v1.5.4 agent context / 测试 / 文档 / TODO 更新 |
| 2 | IT04 caller 关闭后跨进程接力的端到端手测 | 🟡 P1 | 单测已覆盖核心；真人操作体验未走 |
| 3 | DOC03 examples YAML 加注释行 | 🟢 P2 | v1.5.5 时补 |
| 4 | Phase 11 v1.5 增强（10 项） | 🗓 P3 | 等 v1 真实使用反馈触发，详见下表 |
