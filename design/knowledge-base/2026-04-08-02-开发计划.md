# Knowledge Base Skill 开发计划

> **日期**: 2026-04-08
> **关联设计文档**: [设计文档](./2026-04-08-01-设计文档.md)

---

## Phase 1: 核心骨架 ✅

目标：能注册资料、列出索引、基本 SKILL.md 可用。

- [x] 创建 `skills/knowledge-base/` 目录结构
- [x] 编写 `main.py` 命令分发入口（支持 install/update + 业务命令）
- [x] 编写 `config.json`（数据目录路径、默认配置）
- [x] 实现 `src/indexer.py`
  - [x] `add` — 注册单个资料（路径、标题、标签、类型、分类）
  - [x] `add --type directory` — 批量注册目录
  - [x] `list` — 列出索引（支持 --type / --tag / --category / --pending 过滤）
  - [x] `remove` — 移除索引条目
  - [x] `update` — 更新索引元数据
  - [x] `import-project` — 快捷导入当前项目 design/ 目录
- [x] JSONL 索引文件读写工具函数
- [x] 错误处理：路径不存在、编码问题、JSONL 损坏恢复
- [x] 编写 `SKILL.md`（L0 渐进式披露入口）
- [x] 编写 `requirements.txt`
- [x] 初始化 `knowledge-base-data/` 运行时数据目录

## Phase 2: 内容扫描与编译流程 ✅

目标：能扫描索引内容、检测变更、编排编译流程。

- [x] 实现 `src/scanner.py`
  - [x] 根据 type 读取不同类型资料的内容
  - [x] content_hash 计算（md5，检测变更）
  - [x] 内容摘要缓存（`raw/cache/<id>.txt`）
- [x] 实现 `src/compiler.py`
  - [x] `compile` — 生成待编译清单（pending.json）
  - [x] `compile --dry-run` — 预览待编译条目
  - [x] `compile --full` — 全量重编译
  - [x] `compile --finalize` — 后处理（反向链接、图谱、目录重建）
- [x] 编写 `prompts/` LLM 提示词模板
  - [x] `extract_concepts.md` — 概念提取
  - [x] `write_entry.md` — 条目编写
  - [x] `build_relations.md` — 关系建立
- [x] 编译历史记录（`compile/history.jsonl`）

## Phase 3: 浏览、搜索与状态管理 ✅

目标：Agent 能高效浏览和搜索知识库，了解知识库健康状态。

- [x] 实现 `src/browser.py`
  - [x] L1: `browse` — 知识地图概览（分类 + 统计）
  - [x] L2: `browse <category>` — 分类下条目列表
  - [x] L3: `read <concept-id>` — 概念全文
  - [x] L4: `source <source-id>` — 原始资料信息 + 内容预览
- [x] 实现 `src/searcher.py`
  - [x] 关键词搜索（标题 + 内容匹配）
  - [x] 标签筛选
  - [x] 分类筛选
- [x] 实现 `status` 命令 — 健康状态总览
  - [x] 有效/失效索引数
  - [x] 待编译条目数
  - [x] 孤立概念数
  - [x] 最近编译时间
- [x] 实现 `check` 命令 — 路径有效性验证
  - [x] 扫描所有索引，检查路径是否存在
  - [x] 标记失效条目，输出修复建议
- [x] 统一输出格式：结果 + 统计 + 下一步建议

## Phase 4: 知识图谱与概念管理 ✅

目标：可视化概念关联 + 概念/分类的增删改查。

- [x] 实现 `src/graph.py`
  - [x] 从 Wiki 条目构建图谱（nodes + edges）
  - [x] JSON 格式输出
  - [x] Mermaid 格式输出
  - [x] 子图查询（--center + --depth）
- [x] 反向链接索引构建（backlinks.json）
- [x] `[[概念名]]` 内部链接解析
- [x] 概念管理命令
  - [x] `concept remove <id>` — 删除概念条目
  - [x] `concept merge <id1> <id2>` — 合并重复概念
  - [x] `concept rename <id> --title "新名称"` — 重命名概念
- [x] 分类管理命令
  - [x] `category list` — 列出所有分类及统计
  - [x] `category rename <old> <new>` — 重命名分类（批量更新关联条目）

## Phase 5: 增量优化与测试 ✅

目标：增量编译、性能优化、测试覆盖。

- [x] 增量编译策略
  - [x] 基于 content_hash 的变更检测
  - [x] 关联概念级联更新标记
  - [x] 孤立概念检测（所有 source 被删除）
- [ ] Wiki 手动编辑保护
  - [ ] frontmatter 添加 `manual_edit: true` 标记
  - [ ] 编译时跳过手动编辑的条目（或 merge 而非覆盖）
- [ ] 编译报告输出（新增/更新/删除了哪些概念的可读报告）
- [x] 内容缓存机制优化
- [x] CLI 框架升级：pyproject.toml + typer + pydantic
  - [x] 创建 pyproject.toml
  - [x] main.py 用 typer 重构（支持自动补全和彩色输出）
  - [x] 命令定义拆分到 src/commands/ 目录，一个命令一个文件（16 个文件）
  - [x] src/cli.py 精简为纯组装入口，main.py 仅 10 行
- [x] 编写单元测试（`tests/knowledge-base/`）
  - [x] 测试设计文档（`tests/knowledge-base/design/test-design.md`）
  - [x] 测试运行器（`tests/knowledge-base/run_tests.py`）
  - [x] 索引 CRUD 测试（18 个用例）
  - [x] 内容扫描测试（12 个用例）
  - [x] 编译流程测试（8 个用例）
  - [x] 浏览与搜索测试（19 个用例）
  - [x] 知识图谱测试（11 个用例）
  - [x] 运行报告输出到 `tests/knowledge-base/reports/`
  - [x] 共 80 个测试用例，全部通过（含新增 code/config/text/binary 类型检测测试）
- [ ] 大规模索引 SQLite 升级路径（可选）

## Phase 6: 文档与发布 ✅

- [x] 完善 SKILL.md（完整命令文档 + 使用示例 + 资料类型表）
- [x] 更新项目 README.md / README_EN.md 添加 knowledge-base 条目
- [x] 创建 docs/scripts/skills/knowledge-base.js 数据文件
- [x] 更新 docs/index.html + docs/scripts/skills-data.js
- [x] skill-builder 规范检查（install/update/uninstall 已在 src/commands/admin.py）
- [x] 移除 supported_extensions 文件类型限制，支持任意文件
- [x] 补充查询数据流设计
- [x] 编写 12 个自然语言交互测试用例
- [x] 首轮 Wiki 编译（16 个概念，56 节点知识图谱）
- [x] 本地安装验证通过
