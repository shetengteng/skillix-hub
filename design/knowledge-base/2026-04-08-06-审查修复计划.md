# Knowledge Base Skill — 审查修复计划

> **日期**: 2026-04-08
> **来源**: [2026-04-08-05-全局设计审查.md](./2026-04-08-05-全局设计审查.md)

---

## TODO

### P0: 编译质量验证

- [ ] `compiler.py` — `_compile_finalize` 中新增 `_validate_concepts()` 步骤
  - 检查 frontmatter 必填字段（id, title, category）
  - 验证 sources ID 存在于 index.jsonl
  - 验证 related 引用的概念文件存在
  - 输出警告列表（不阻塞流程）

### P1: 绝对路径 → 相对路径

- [ ] `indexer.py` — `cmd_add` / `_add_directory` / `cmd_import_project` 存储相对于项目根的路径
- [ ] `indexer.py` — `read_index` 增加路径解析（相对→绝对）
- [ ] 新增 `kb migrate --old-base --new-base` 命令批量修复路径
- [ ] 兼容处理：读取时自动识别绝对/相对路径

### P1: 知识陈旧感知

- [ ] `browser.py` — `cmd_read` 输出时检查关联 sources 的 content_hash 是否变化，标注 stale
- [ ] `searcher.py` — `cmd_status` 增加"N 个概念来源已变更"统计

### P2: 孤立概念检测

- [ ] `compiler.py` — `_compile_finalize` 中检测 orphan concepts（sources 全不在 index 中）
- [ ] 输出警告提示

### P2: 编译产物备份

- [ ] `compiler.py` — `_compile_prepare` 执行前自动备份 `wiki/` 到 `wiki.bak/`

### P3: 分类推断增强

- [ ] `indexer.py` — `infer_category` 扩展规则：`skills/<name>/`、`docs/<name>/`、`tests/<name>/`

### 测试

- [ ] 为所有新功能编写单元测试
- [ ] 运行全量测试确认无回归
