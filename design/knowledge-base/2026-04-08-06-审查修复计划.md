# Knowledge Base Skill — 审查修复计划

> **日期**: 2026-04-08
> **来源**: [2026-04-08-05-全局设计审查.md](./2026-04-08-05-全局设计审查.md)

---

## TODO

### P0: 编译质量验证 ✅

- [x] `compiler.py` — `_compile_finalize` 中新增 `_validate_concepts()` 步骤
  - 检查 frontmatter 必填字段（id, title, category）
  - 验证 sources ID 存在于 index.jsonl
  - 验证 related 引用的概念文件存在
  - 输出警告列表（不阻塞流程）

### P1: 绝对路径 → 相对路径 ✅

- [x] `indexer.py` — `cmd_add` / `_add_directory` / `cmd_import_project` 存储相对于项目根的路径
- [x] `indexer.py` — 新增 `resolve_path()` 兼容层（自动识别绝对/相对路径）
- [x] 新增 `kb migrate --old-base --new-base --to-relative --dry-run` 命令
- [x] `scanner.py` / `searcher.py` / `browser.py` 所有文件访问改用 `resolve_path()`

### P1: 知识陈旧感知 ✅

- [x] `browser.py` — `cmd_read` 输出时检查关联 sources 的 content_hash 是否变化，标注 stale
- [x] `searcher.py` — `cmd_status` 增加"来源已变更: N"统计 + 重编译建议

### P2: 孤立概念检测 ✅

- [x] `compiler.py` — `_compile_finalize` 中 `_detect_orphan_concepts()` 检测 sources 全不在 index 中的概念
- [x] 输出警告提示

### P2: 编译产物备份 ✅

- [x] `compiler.py` — `_compile_prepare` 执行前 `_backup_wiki()` 备份 `wiki/` 到 `wiki.bak/`

### P3: 分类推断增强 ✅

- [x] `indexer.py` — `infer_category` 扩展：识别 `skills/`、`docs/`、`tests/`、`src/`、`packages/`、`modules/` 目录

### 测试 ✅

- [x] 81 个单元测试全部通过（含搜索重构的 20 个新测试）
- [x] 功能验证：search / status / migrate --dry-run 均正常
