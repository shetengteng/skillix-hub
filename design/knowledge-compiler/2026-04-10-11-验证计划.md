# Knowledge Compiler — 真实项目验证计划

> 日期：2026-04-10
> 测试对象：`metadata-server` 项目（/Users/TerrellShe/Documents/projects/data-governance-metadata/）
> 测试文档：63 个 Markdown 文件（设计文档、API 文档、迁移计划、README 等）
> 验证目标：确认 MVP 在真实项目中的端到端可用性

---

## Phase 1：初始化与添加

- [x] **T1.1** `kc init` 在 metadata-server 项目目录成功执行
  - 验证：raw/、wiki/、.kc-config.json、wiki/INDEX.md、wiki/schema.md、wiki/log.md 都存在 ✅
- [x] **T1.2** `kc add` 批量添加 doc/ 目录下的设计文档
  - 验证：2 个文件被复制到 raw/designs/ ✅
- [x] **T1.3** `kc add` 递归添加 tools/ 下的多层文档
  - 验证：42 个文件添加到 raw/research/，子目录结构保留 ✅
- [x] **T1.4** 分类推断是否合理
  - 验证：--category designs 指定生效，recursive 默认进 research/ ✅

---

## Phase 2：编译核心

- [x] **T2.1** `kc compile --dry-run` 预览分类结果
  - 验证：输出 33 个概念列表 + 每个文件的分类方法/置信度 ✅
  - 发现：首次编译全部为 new_topic/low（符合预期）
- [x] **T2.2** `kc compile` 增量编译
  - 验证：wiki/concepts/ 下生成 33 个概念文章 ✅
  - 发现：12 个 readme.md 被合并为一个"readme"概念（分类策略合理）
  - 修复：首次发现 nonempty_content/coverage_tags Hard Gate 对 stub 过于严格，已修复为仅结构性 Gate 阻断
- [x] **T2.3** 概念文章结构是否完整
  - 验证：YAML frontmatter（id/title/sources/relations/created/updated/compile_count）✅
  - 验证：各 section 存在 ✅，coverage 标记为 low（首次编译符合预期）✅
  - 验证：来源引用 [source: raw/...] 正确 ✅
- [x] **T2.4** Hard Gate 是否触发阻断
  - 验证：结构性 Gate（frontmatter/source_refs/schema）无失败 ✅
  - 内容质量 Gate 降级为警告（57 项，首次编译 stub 预期行为）✅
- [x] **T2.5** schema.md 是否自动更新
  - 验证：33 个新概念出现在 Uncategorized 中 ✅

---

## Phase 3：增量编译 + 用户编辑保留

- [ ] **T3.1** 手动编辑一篇概念文章（添加自定义 section 或修改内容）
- [ ] **T3.2** 再次添加新文档到 raw/ 并 `kc compile`
  - 验证：手动编辑的内容被保留
  - 验证：新来源被正确合并
- [ ] **T3.3** 检查 frontmatter 生命周期
  - 验证：created 保持不变
  - 验证：updated 更新为今天
  - 验证：compile_count 递增

---

## Phase 4：知识问答

- [x] **T4.1** `kc query "metadata-server 的 tenant 管理方案"` 能找到相关概念
  - 验证：返回 5 个相关概念（set-trino-tenant、all-tenant-list 等）✅
  - 验证：low coverage section 有 ⚠️ 提示 ✅
- [ ] **T4.2** `kc query "数据迁移方案" --save` 保存查询草稿
  - 待执行

---

## Phase 5：质量检查

- [x] **T5.1** `kc lint` 输出质量报告
  - 验证：Hard 108 pass / 57 fail | Soft 128 pass / 70 warn ✅
  - 验证：Soft Gate 包含 stale_source、missing_cross_refs、low_coverage ✅
- [ ] **T5.2** `kc lint --fix` 自动修复 schema 同步
  - 待执行
- [x] **T5.3** `kc status` 显示统计信息
  - 验证：44 source files, 33 concepts, 165 sections all low coverage ✅
  - 验证：33 weak concepts（首次编译预期行为）✅
- [x] **T5.4** `kc browse` 浏览知识地图
  - 验证：33 concepts 在 Uncategorized 下按字母排序展示 ✅

---

## Phase 6：Auto-relations + Related

- [ ] **T6.1** 检查 frontmatter relations.related 是否自动填充
  - 验证：如果概念 A 正文提到概念 B 的 slug，A 的 relations.related 应包含 B
- [ ] **T6.2** 检查 ## Related 正文是否自动生成
  - 验证：Related 中有 [[slug]] 格式的链接

---

## Phase 7：AI Agent 管道验证

- [ ] **T7.1** 读取 `.kc-compile-prompt.md`，确认 prompt 可用
  - 验证：prompt 包含来源全文、编译指令、矛盾处理指令
- [ ] **T7.2** 模拟 AI 编写一篇概念文章，使用 `kc apply` 写回
  - 验证：section merge 保留了之前的内容
  - 验证：文章质量从 low coverage 升级

---

## 通过标准

- **必须全部通过**：T1.1-T1.4, T2.1-T2.5, T3.1-T3.3, T5.1-T5.4
- **强烈建议通过**：T4.1-T4.2, T6.1-T6.2, T7.1-T7.2
- **总测试项**：20 项
- **最低通过率**：16/20（80%）
