# Knowledge Compiler — 开发 TODO

> 日期：2026-04-10
> 基于：`2026-04-10-05-新方案设计.md` (v2)

---

## P0 — Skill 骨架 + init + add ✅

- [x] 创建 `skills/knowledge-compiler/` 目录结构
- [x] 编写 `SKILL.md`（~150 行精简入口）
- [x] 编写 `pyproject.toml`（typer 依赖）
- [x] 编写 `config.json`（默认配置）
- [x] 实现 `main.py`（typer CLI 入口）
- [x] 实现 `commands/init.py`
  - [x] 创建 `raw/` 子目录（designs, decisions, research, notes, assets）
  - [x] 创建 `wiki/` 目录
  - [x] 生成 `.kc-config.json`
  - [x] 生成 `wiki/INDEX.md`（空脚手架）
  - [x] 生成 `wiki/schema.md`（空脚手架）
  - [x] 生成 `wiki/log.md`（空）
- [x] 实现 `commands/add.py`
  - [x] 添加单个文件（复制到 raw/）
  - [x] 添加目录（`--recursive` + `--pattern`）
  - [x] 支持 `--tags` 和 `--category`
- [x] 编写模板文件
  - [x] `templates/article.md`
  - [x] `templates/schema.md`
  - [x] `templates/index.md`
- [x] **验证**：`kc init && kc add doc.md` 成功执行

---

## P1 — 编译核心（Phase 1-3）✅

- [x] 实现 `scanner.py`（Phase 1）
  - [x] 读取 `.compile-state.json`
  - [x] 遍历 `raw/` 目录，对比 mtime
  - [x] 输出 `new_files`, `changed_files`, `deleted_files`
  - [x] `--full` 模式：全部视为 changed
- [x] 实现 `classifier.py`（Phase 2）
  - [x] 读取变更文件的标题 + 前 500 字
  - [x] 推断 topic slug（lowercase-kebab-case）
  - [x] 匹配已有概念（优先合并）
  - [x] 新主题创建
  - [ ] 歧义时交互确认（待 AI 集成后完善）
  - [x] 输出 `topic_map {slug: [file_paths]}`
- [x] 编写 `prompts/classify_topics.md`
- [x] 实现 `compiler.py`（Phase 3）
  - [x] 读取概念所有来源全文
  - [x] 生成概念骨架文章（基于模板）
  - [x] 覆盖度标记（默认 low，AI 编译时精细标记）
  - [x] 来源引用 `[source: path]`
  - [x] 生成编译 prompt 供 AI Agent 执行精细编译
  - [ ] 支持并行（待后续优化）
- [x] 编写 `prompts/compile_article.md`
- [x] 实现 `commands/compile.py`
  - [x] `kc compile`（增量）
  - [x] `kc compile --full`（全量）
  - [x] `kc compile --dry-run`（预览）
  - [x] `kc compile --topic <slug>`（单概念）
- [x] **验证**：3 篇文档编译出 3 个概念，增量编译正常，dry-run 正常

---

## P2 — 完整编译闭环（Phase 3.5-5）

- [ ] 实现 `schema.py`（Phase 3.5）
  - [ ] 首次运行生成 `wiki/schema.md`
  - [ ] 增量更新：新增主题 → 更新 taxonomy
  - [ ] 不覆盖用户手动编辑部分
- [ ] 实现 `indexer.py`（Phase 4）
  - [ ] 生成 `wiki/INDEX.md`
  - [ ] 分类聚合（基于 schema.md 分类）
  - [ ] 覆盖度摘要
  - [ ] 更新标记 `[updated: YYYY-MM-DD]`
- [ ] 实现 `verifier.py`（Phase 4）
  - [ ] Hard Gates 检查（5 项）
  - [ ] Soft Gates 检查（6 项）
  - [ ] 输出检查报告
- [ ] 实现 `state.py`（Phase 5）
  - [ ] 更新 `.compile-state.json`（mtime 快照）
  - [ ] 追加 `wiki/log.md`（编译摘要）
- [ ] **验证**：INDEX.md 正确反映所有概念，schema.md 包含所有分类

---

## P3 — 知识问答

- [ ] 实现 `commands/query.py`
  - [ ] 读取 INDEX.md 定位相关概念
  - [ ] 读取概念文章
  - [ ] low 覆盖章节 → 自动回查 raw/ 来源
  - [ ] 合成回答 + 引用
  - [ ] `--save` 保存为新概念文章
- [ ] **验证**：问 5 个问题，回答引用正确的概念和来源

---

## P4 — 健康检查

- [ ] 实现 `commands/lint.py`
  - [ ] Hard Gate 报告
  - [ ] Soft Gate 报告
  - [ ] 过期检测（mtime + 30 天时间窗口）
  - [ ] `--fix` 自动修复安全问题（orphan 链接、schema 同步）
- [ ] **验证**：能检出孤立概念、断裂链接、过期文档

---

## P5 — 会话注入

- [ ] 实现 `session/injector.py`
  - [ ] 向上查找 `.kc-config.json`
  - [ ] 根据 `session_mode` 注入上下文
  - [ ] 未找到时静默退出
- [ ] 适配 Cursor hook 机制
- [ ] **验证**：进入项目时自动识别 Wiki 并注入

---

## P6 — 状态与浏览

- [ ] 实现 `commands/status.py`
  - [ ] 概念总数 + 分类统计
  - [ ] 覆盖度分布（high/medium/low 各占比）
  - [ ] 过期概念列表
  - [ ] 上次编译时间
- [ ] 实现 `commands/browse.py`
  - [ ] 知识地图概览（分类 → 概念列表）
  - [ ] `browse <category>` 查看分类详情
- [ ] **验证**：status 输出准确反映知识库状态

---

## 最小闭环验证节点

P0 + P1 完成后，用团队真实的 10-20 篇设计文档测试 `init → add → compile`：

- [ ] 概念发现是否合理
- [ ] 覆盖度标记是否准确
- [ ] 来源引用是否正确
- [ ] 用户手动编辑后再编译，编辑是否保留
