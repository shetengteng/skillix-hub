# Knowledge Compiler — 后续增强 TODO

> 日期：2026-04-10
> 说明：本清单只记录当前代码已形成 AI-first MVP 之后，仍值得继续推进的增强项。

---

## P1 — 编译闭环增强

- [ ] 评估是否增加 `kc compile --auto`
- [ ] 如果增加 `--auto`，明确 LLM 接入方式、模型依赖、失败回退策略
- [x] 明确 `compile -> apply` 管道在 `SKILL.md` 中的标准用法（已完成 4 种使用模式）
- [ ] 评估是否需要批量 apply 能力，支持一次写回多个概念

---

## P2 — 分类体验增强

- [ ] 设计非阻塞式歧义处理方案，避免交互式 prompt 阻塞 AI Agent
- [x] 强化 dry-run 输出，让分类依据更可解释（输出方法/置信度/原始slug）
- [x] 当分类置信度低时，输出候选 topic 建议（_find_candidates 已实现）
- [ ] 评估是否需要记录分类决策日志，便于回溯误分类

---

## P3 — 质量检查增强

- [ ] 设计 `lint --deep` 模式
- [ ] 在 deep 模式中评估引入 AI 驱动的“内容矛盾”检查
- [ ] 定义“内容矛盾”的最小判定标准，避免误报
- [ ] 明确 deep 模式与默认 lint 的性能和成本边界

---

## P4 — Query 能力增强

- [ ] 将 `query --save` 从“保存草稿概念”升级为“可选保存 AI 整理后的概念”
- [ ] 评估 query 阶段是否需要输出更强的引用摘要
- [x] query 结果自动同步 schema / index（query.py --save 后自动调用 update_schema + update_index）

---

## P5 — 文档与工作流增强

- [x] 在 `SKILL.md` 中补充 AI-first 标准工作流示例（4 种模式）
- [x] 在设计文档中明确 `compile/apply` 是架构选择（一致性检查 6.1 已说明）
- [x] 补一份“团队真实文档验收记录”（验证计划.md，metadata-server 44 篇文档）
- [x] 总结一组推荐的使用模式（已写入 SKILL.md AI Agent 标准工作流章节）

---

## P6 — 真实验收

- [x] 用真实团队设计文档验证概念发现质量（metadata-server 44 篇 -> 33 概念）
- [x] 验证 coverage 标记是否可用（首次编译全 low，符合预期）
- [x] 验证来源引用是否准确（[source: raw/...] 正确指向）
- [ ] 验证用户手动编辑后的重编译保留效果
- [ ] 验证 AI Agent 是否能稳定完成 `compile -> apply -> lint` 流程
