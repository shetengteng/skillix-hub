# 自然语言时间范围查询设计

> 日期：2026-02-20
> 状态：设计中

## 1. 需求背景

用户希望通过自然语言询问"最近1个月做了什么"这类时间范围查询。当前系统部分支持此功能，需要补齐缺失部分并在 SKILL.md / rules 中添加引导规则。

## 2. 现状分析

### 2.1 已支持时间范围的命令

| 命令 | 参数 | 说明 |
|---|---|---|
| `manage list` | `--days N` | 最近 N 天 |
| `manage list` | `--from YYYY-MM-DD` | 起始日期 |
| `manage list` | `--to YYYY-MM-DD` | 结束日期 |
| `manage list` | `--before YYYY-MM-DD` | 早于某日 |
| `manage delete` | `--from`、`--to`、`--before` | 同上 |

### 2.2 不支持时间范围的命令

| 命令 | 现有参数 | 缺失 |
|---|---|---|
| `search_memory.py` | `query`、`--max-results`、`--method` | 无 `--days`/`--from`/`--to` |
| `manage export` | `--output`、`--scope`、`--type` | 无时间过滤 |

### 2.3 规则引导缺失

`SKILL.md` 和 `memory-rules.mdc` 中没有说明如何响应时间范围查询，Agent 不知道该用哪个命令。

## 3. 设计方案

### 3.1 方案概述

分三层实现：

```
用户自然语言 → Agent 解析意图 → 选择合适命令 → 返回结果
                    ↑
              SKILL.md 规则引导
```

### 3.2 SKILL.md 新增规则

在 SKILL.md 的"手动行为"部分新增"时间范围查询"章节：

```markdown
### 用户询问时间范围内的活动

当用户问"最近 N 天/周/月做了什么"、"上周的记忆"等时间范围查询时：

**意图映射：**

| 用户可能的说法 | 对应命令 |
|---|---|
| "最近1个月做了什么" | `manage list --days 30` |
| "最近一周的记忆" | `manage list --days 7` |
| "上个月的决策" | `manage list --from 2026-01-01 --to 2026-01-31 --type W` |
| "2月份做了什么" | `manage list --from 2026-02-01 --to 2026-02-28` |
| "今天的记忆" | `manage list --days 1` |
| "搜索最近一周关于数据库的记忆" | `search_memory.py "数据库" --days 7`（需扩展） |

**执行流程：**

1. 解析用户意图，确定时间范围和可选关键词
2. 如果只需要列出/浏览 → 使用 `manage list`
3. 如果需要语义搜索 + 时间过滤 → 使用 `search_memory.py`（扩展后）
4. 格式化输出，按时间倒序展示
```

### 3.3 search_memory.py 扩展

新增可选参数：

```python
parser.add_argument("--days", type=int, help="限制搜索最近 N 天的记忆")
parser.add_argument("--from", dest="from_date", help="起始日期 YYYY-MM-DD")
parser.add_argument("--to", help="结束日期 YYYY-MM-DD")
```

在 `sqlite_search.py` 的搜索函数中，对 `chunks.timestamp` 添加 WHERE 条件：

```python
def _build_time_filter(days=None, from_date=None, to_date=None):
    """构建时间过滤 SQL 条件"""
    conditions = []
    params = []
    if days:
        cutoff = (datetime.utcnow() - timedelta(days=days)).strftime("%Y-%m-%dT%H:%M:%S")
        conditions.append("timestamp >= ?")
        params.append(cutoff)
    if from_date:
        conditions.append("timestamp >= ?")
        params.append(from_date + "T00:00:00")
    if to_date:
        conditions.append("timestamp <= ?")
        params.append(to_date + "T23:59:59")
    return conditions, params
```

影响范围：
- `search_fts()` - 全文搜索添加时间过滤
- `search_vector()` - 向量搜索添加时间过滤
- `hybrid_search()` - 混合搜索添加时间过滤

### 3.4 manage export 扩展

新增时间过滤参数：

```python
p = sub.add_parser("export")
p.add_argument("--days", type=int)
p.add_argument("--from", dest="from_date")
p.add_argument("--to")
```

在 `cmd_export.py` 中复用 `filter_entries()` 做日期过滤。

### 3.5 memory-rules.mdc 更新

在规则文件中添加时间范围查询的触发词：

```
触发词：最近、上周、上个月、这周、今天、N天前、时间范围
```

## 4. 实现优先级

| 优先级 | 任务 | 工作量 |
|---|---|---|
| P0 | SKILL.md 添加时间范围查询规则 | 小 |
| P0 | memory-rules.mdc 添加触发词 | 小 |
| P1 | search_memory.py 添加时间过滤参数 | 中 |
| P2 | manage export 添加时间过滤 | 小 |

P0 任务仅需修改文档/规则，Agent 即可通过 `manage list --days N` 响应时间范围查询。

P1 任务扩展 `search_memory.py`，使"搜索最近一周关于 XX 的记忆"这种组合查询成为可能。

## 5. 交互示例

### 示例 1：简单时间范围查询

```
用户：最近1个月做了什么？

Agent 执行：
  python3 manage/index.py list --days 30

Agent 回复：
  最近 30 天的记忆记录：

  📅 2026-02-20
  • [W] 实现了 .memory-disable 项目级禁用功能
  • [W] 添加了 MEMORY.md 自动初始化逻辑

  📅 2026-02-19
  • [S] 会话摘要：测试代码整合重构
  • [W] 统一了测试目录结构

  📅 2026-02-18
  • [W] 完成了 Memory Skill 安装脚本
  ...

  共 47 条记录，涵盖 15 个会话。
```

### 示例 2：带关键词的时间范围查询（P1 实现后）

```
用户：搜索最近一周关于数据库的讨论

Agent 执行：
  python3 search_memory.py "数据库" --days 7

Agent 回复：
  最近 7 天关于"数据库"的记忆：

  1. [W] 项目统一使用 PostgreSQL（置信度 0.95）
     日期: 2026-02-18

  2. [W] 数据库连接池最大 20 连接
     日期: 2026-02-17

  共 2 条相关记忆。
```

## 6. 扩展场景：基于时间范围的报告生成

除了简单的列出和搜索，用户还可能基于时间范围的记忆数据提出更高级的需求，例如生成工作报告、周报、月度总结等。

### 6.1 典型场景

| 用户可能的说法 | 意图 | 执行策略 |
|---|---|---|
| "帮我生成这个月的工作报告" | 汇总 + 分类 + 格式化 | `manage list --days 30` → Agent 汇总 |
| "给我一个上周的周报" | 按天分组 + 摘要 | `manage list --days 7` → Agent 按天整理 |
| "总结一下最近做的事情" | 概要提取 | `manage list --days 14` → Agent 提炼要点 |
| "这个月的决策有哪些" | 按类型过滤 | `manage list --days 30 --type W` → Agent 筛选决策 |
| "最近一个月的项目进展" | 项目维度汇总 | `manage list --days 30` → Agent 按项目分组 |

### 6.2 Agent 处理流程

```
用户："帮我生成这个月的工作报告"
        ↓
Agent 解析意图：时间范围 = 30天，输出格式 = 报告
        ↓
Agent 执行：manage list --days 30
        ↓
Agent 获取原始记忆数据
        ↓
Agent 自身能力：
  1. 按日期分组
  2. 按类型分类（W=决策, B=偏好, O=观察, S=摘要）
  3. 提炼关键成果
  4. 生成结构化报告
        ↓
输出格式化报告
```

### 6.3 交互示例

```
用户：帮我生成这个月的工作报告

Agent 执行：
  python3 manage/index.py list --days 30

Agent 回复：
  📊 2026年2月工作报告

  ## 关键成果
  • 完成 Memory Skill 项目级禁用功能（.memory-disable）
  • 实现新项目 MEMORY.md 自动初始化
  • 完成自然语言时间范围查询设计
  • 统一测试目录结构，128 个测试全部通过

  ## 技术决策
  • [W] memory-data 始终保持项目级存储
  • [W] 禁用机制使用 .cursor/skills/.memory-disable 标记文件
  • [W] 允许禁用状态下 memory-data 目录完整创建

  ## 按日期明细
  📅 2026-02-20（6 条）
  • 实现 .memory-disable 项目级禁用功能
  • 添加 MEMORY.md 自动初始化逻辑
  ...

  📅 2026-02-19（4 条）
  • 测试代码整合重构
  ...

  共 47 条记录，涵盖 15 个会话。
```

### 6.4 设计要点

- **无需额外命令**：报告生成依赖 Agent 自身的文本整理能力，底层数据通过 `manage list` 获取
- **SKILL.md 规则引导**：在规则中说明当用户请求"报告/周报/总结"时，先获取数据再格式化输出
- **可选 export 配合**：对于需要保存的报告，可以先 `manage export --days 30` 导出数据，再由 Agent 生成报告文档

## 7. 风险与注意事项

- `manage list --days 30` 可能返回大量数据，需要配合 `--limit` 使用
- 时间过滤基于 `timestamp` 字段，格式为 ISO 8601，需确保一致性
- `search_memory.py` 的时间过滤在 SQLite 层面实现，不影响向量搜索的召回率
